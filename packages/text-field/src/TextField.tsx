// Radix Themes' TextField is a compound component (TextField.Root, TextField.Slot).
// Re-export the namespace directly so `<TextField.Root>` / `<TextField.Slot>` work
// the same way they do via `@radix-ui/themes`.

export { TextField } from '@radix-ui/themes';
