---
"@pineappleui/tokens": minor
---

First public release of `@pineappleui/tokens`: the accent-color list and the theme-preference types, as pure data with no React, Radix or DOM dependency. `ACCENT_COLORS` leads with `bronze`, the default accent.
